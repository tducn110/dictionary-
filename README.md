
  # font-of-intent (Community)

  This is a code bundle for font-of-intent (Community). The original project is available at https://www.figma.com/design/1mqgjCY9c2UlNfmF55RibG/font-of-intent--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  